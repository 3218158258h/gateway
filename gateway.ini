# 网关总配置清单
# 这里只负责指向各层配置文件，不承载具体业务参数。

[config_files]
transport = config/transport.ini
physical_transport = config/transport_physical.ini
runtime = config/runtime.ini
router = config/router.ini
persistence = config/persistence.ini
device = config/device.ini
network = config/network.ini
protocols = config/protocols.ini
