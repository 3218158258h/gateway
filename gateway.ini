# 网关总配置清单

[config_files]
# 网络协议层配置文件，负责 MQTT/DDS 连接与主题。
transport = config/transport.ini
# 物理传输层配置文件，负责 UART/CAN/SPI/I2C 参数。
physical_transport = config/transport_physical.ini
# 私有协议定义文件，负责帧格式、ACK/NACK 和初始化命令模板。
protocols = config/protocols.ini
# 守护进程配置文件，负责子进程拉起与重启策略。
daemon = config/daemon.ini

[runtime]
# 线程池执行器数量，控制后台任务并发度。
thread_pool_executors = 4

[router]
# 路由缓冲区上限，决定单条消息的最大承载能力。
max_message_size = 4096

[persistence]
# SQLite 消息数据库路径。
db_path = /home/nvidia/gateway/messages.db
# 失败后最多重试次数。
max_retry = 3
# 消息过期时间（小时）。
expire_hours = 24
# 本地待发送消息队列上限。
max_queue_size = 20000

[device]
# 设备总数上限，超过后忽略多余条目。
max_devices = 32
# 设备心跳超时（秒）。
heartbeat_timeout = 30
# 设备读写缓冲区大小（字节）。
buffer_size = 16384
# 设备节点清单迁移到 config/transport_physical.ini 的 [device_registry]。

